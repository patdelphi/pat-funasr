# Pat FunASR 全面探查报告与修复计划

> 生成时间：2026-06-11
> 探查范围：API服务端、Gradio WebUI、测试覆盖、文档对齐

---

## 一、问题总览

| 类别 | 高 | 中 | 低 | 合计 |
|------|---|---|---|------|
| API服务端（server.py等） | 3 | 6 | 8 | 17 |
| Gradio WebUI（gradio_app.py等） | 2 | 6 | 4 | 12 |
| 文档与todo.md | 0 | 5 | 1 | 6 |
| 测试覆盖 | 0 | 2 | 0 | 2 |
| **合计** | **5** | **19** | **13** | **37** |

---

## 一、API服务端问题（server.py / renderers.py / segmentation.py / vad_presets.py）

### 🔴 高严重度（3项）

| # | 文件:行号 | 问题 | 修复建议 |
|---|-----------|------|----------|
| H1 | `server.py:1294` | `result[0]` 无空列表保护，`asr_model.generate()` 返回空时抛 IndexError | 加 `if result else ""` 保护，与1415行保持一致 |
| H2 | `server.py:388-450` | `load_model` 在 async 端点中同步调用，MODEL_REGISTRY 检查-写入非原子，并发请求可重复加载同一模型 | 加 `threading.Lock` 或用 `asyncio.Lock` 保护 |
| H3 | `server.py:1621-1630` | `/health` 端点暴露 `model_variants_loaded`（含 device/hub/log_level），信息泄露 | 只返回已加载模型主名列表，不暴露内部 key |

### 🟡 中严重度（6项）

| # | 文件:行号 | 问题 | 修复建议 |
|---|-----------|------|----------|
| M1 | `server.py:54-97` | `_dbg_report` 用 `urllib.request.urlopen` 同步阻塞，会阻塞 FastAPI asyncio 事件循环 | 改用 `asyncio.to_thread` 或仅在后台线程发送 |
| M2 | `server.py:905` | 内嵌HTML中使用已弃用 `createScriptProcessor`，应迁移到 AudioWorkletNode | 评估是否需要迁移（当前仅备用页面，可标记TODO） |
| M3 | `server.py:1190-1192` 等3处 | 临时文件 `delete=False` + `finally os.unlink`，进程被 kill 时文件泄漏 | 改用 `tempfile.TemporaryDirectory()` 上下文管理器 |
| M4 | `server.py:1191` | 上传文件无大小限制，`await file.read()` 一次性读入内存，可被恶意大文件 OOM | 加文件大小上限校验（如 2GB） |
| M5 | `server.py:338-339` | `punc_mode=disabled` 对内建标点模型（sensevoice/fun-asr-nano/qwen3-asr）静默忽略，用户无感知 | 返回 warning 日志或在响应中提示 |
| M6 | `segmentation.py:68-70` | `_to_seconds` 用 `>=1000` 判断毫秒，1000~9999秒的音频时间戳会被错误除以1000 | 优化启发式逻辑或改用类型提示区分 |

### 🟢 低严重度（8项）

| # | 文件:行号 | 问题 |
|---|-----------|------|
| L1 | `server.py:239` | `STREAMING_SESSION_TTL_S` 硬编码 3600，无环境变量入口 |
| L2 | `server.py:1571` | `/v1/models` 返回 `"created": 1700000000` 假时间戳 |
| L3 | `server.py:48-100` | `_dbg_report` 调试代码残留（hardcoded session_id/run_id） |
| L4 | `segmentation.py:45` | 文本回退分段硬编码 `chunk_size=30` |
| L5 | `server.py:1635` | `--host` 默认 `0.0.0.0`，生产应默认 `127.0.0.1` |
| L6 | `server.py:1638` | `--model` 参数解析后从未使用，死代码 |
| L7 | `renderers.py:19-26` | `_clamp_seconds` 仅下限截断，无上限截断 |
| L8 | `server.py:475-478` | `merge_streaming_text` O(n*m) 重叠检测，长文本性能差 |

---

## 二、Gradio WebUI问题（gradio_app.py / app_utils.py）

### 🔴 高严重度（2项）

| # | 文件:行号 | 问题 | 修复建议 |
|---|-----------|------|----------|
| G-H1 | `gradio_app.py:1497` | `check_service_and_capabilities` 返回类型标注 `tuple[str,str,str]`（3个），实际返回 4 个值，与 3017-3021 行 outputs 绑定不一致 | 修正类型标注为 `tuple[str,str,str,str]` |
| G-H2 | `gradio_app.py:1484` | `build_service_dashboard_snapshot` 中 `_, status_text, models = fetch_model_choices(...)` 丢弃 choices，自动刷新时不更新模型状态文本 | 保留 choices 并输出到 model_status 组件 |

### 🟡 中严重度（6项）

| # | 文件:行号 | 问题 | 修复建议 |
|---|-----------|------|----------|
| G-M1 | 多处（~30处） | `gr.update(value=..., choices=...)` 遗留模式，Gradio 5+ 推荐直接返回组件构造函数 | 逐步迁移到 `gr.Dropdown(choices=..., value=...)` 形式 |
| G-M2 | `gradio_app.py:2864-2869` | `gr.Audio(streaming=True)` 与 `sources=["microphone"]` 重复 | 移除冗余 `streaming=True` |
| G-M3 | `gradio_app.py:2864-2869` | `recording=False` 静态设置无实际意义 | 移除或改为注释说明 |
| G-M4 | `gradio_app.py:177` 等多处 | `_dbg_report` / `_dbg_get_server_config` 中 `except Exception: pass` 完全吞异常 | 至少加 logger.debug 记录 |
| G-M5 | `gradio_app.py:36-40` | 运行时 `sys.path.insert` 操控脆弱，可能模块名冲突 | 改用相对导入或包内导入 |
| G-M6 | `gradio_app.py:72` | `import renderers` 跨目录隐式依赖 sys.path，部署易遗漏 | 将 renderers.py 复制到 pat_funasr_webui/ 或改为显式依赖声明 |

### 🟢 低严重度（4项）

| # | 文件:行号 | 问题 |
|---|-----------|------|
| G-L1 | `gradio_app.py:3306-3330` | `main()` 中 asyncio 初始化 Windows 事件循环策略设置失败时静默 pass |
| G-L2 | `gradio_app.py:882` 等多处 | `finally` 块中清理异常被吞，可能留僵尸进程 |
| G-L3 | `gradio_app.py:2479-2482` | `format_streaming_preview_text(full_text, final_flag)` 中 `final_flag` 参数被 `_ = final_flag` 丢弃 |
| G-L4 | `gradio_app.py:2689` | `gr.State(False)` 无类型约束，后续当作 bool 使用 |

---

## 三、文档与todo.md问题

### 文档不一致（5项）

| # | 文件 | 问题 |
|---|------|------|
| D1 | `Docs/api.md:126-127` | streaming端点 `encoder_chunk_look_back` 默认值写0，代码实际为4；`decoder_chunk_look_back` 文档写0，代码实际为1 |
| D2 | `Docs/model-capability-matrix.md:29-33` | 端点列表只写了3个，遗漏5个已实现端点（streaming/diarization/emotion/model-status/model-load） |
| D3 | `Docs/model-capability-matrix.md:9` | 模型别名列表缺少 `paraformer-zh-streaming` 和 `emotion2vec-plus-large` |
| D4 | `Docs/model-capability-matrix.md:133-135` | 参数白名单建议仍按早期口径（`max_words_per_line`/`max_line_count`），代码中实际未实现 |
| D5 | `app/openai_api/openapi.json` | 是否已同步新增端点未验证 |

### todo.md状态失真（5项）

| # | 位置 | 问题 |
|---|------|------|
| T1 | `todo.md:58-120` | 专项计划任务1-6实际已完成（sensevoice已移除punc_model、dbg_report已加开关、batch_size_threshold_s已接入等），但未标记 `[x]` |
| T2 | `todo.md:35-36` | "当前进行中"列出A/B阶段未完成，但执行顺序中A0/A1/B1-B5已标完成，矛盾 |
| T3 | `todo.md:209` | A2标 `[ ]` 未完成，但B阶段依赖A2且已全部 `[x]`，逻辑矛盾 |
| T4 | `todo.md:401` | Streaming参数标 `[ ]`，但server.py中streaming端点已实现这些参数 |
| T5 | `todo.md:103` | openapi.json需补齐端点，状态未验证 |

---

## 四、测试覆盖缺口

| # | 问题 | 修复建议 |
|---|------|----------|
| P1 | **GET `/health` 无端点级测试** | 新增 test_server_health_endpoint.py |
| P2 | **GET `/v1/models` 无端点级测试** | 新增或扩展 test_model_configs.py 覆盖 |

---

## 五、修复优先级建议

### P0（必须修复 — 会导致运行时错误或数据错误）
1. **H1** — result[0] 空列表保护（会导致500错误）
2. **G-H1** — 返回类型标注与实际不一致（可能导致Gradio绑定错误）
3. **D1** — streaming文档默认值错误（导致客户端误配置）

### P1（建议修复 — 影响稳定性或安全性）
4. **H2** — load_model 并发竞态
5. **H3** — health端点信息泄露
6. **M4** — 上传文件无大小限制
7. **M6** — _to_seconds 时间戳边界歧义
8. **G-H2** — 自动刷新不更新模型状态

### P2（可延后 — 技术债与优化）
9. **M1** — _dbg_report 阻塞事件循环
10. **M3** — 临时文件泄漏风险
11. **G-M1** — gr.update() 逐步迁移
12. **G-M5/M6** — sys.path / 跨模块依赖
13. **D2-D5** — 文档对齐
14. **T1-T5** — todo.md 状态清理
15. **P1/P2** — 测试覆盖补充

### P3（可选优化）
16. L1-L8, G-L1-G-L4 — 低优先级改进项

---

## 六、验证方案

修复完成后执行：
```bash
# 全量单测回归
python -m pytest tests/ -q

# 重点关注的测试文件
python -m pytest tests/test_model_configs.py tests/test_server_generate_kwargs.py tests/test_server_transcriptions_endpoint.py tests/test_server_streaming_endpoint.py tests/test_server_diarization_endpoint.py tests/test_server_emotion_endpoint.py tests/test_pat_webui_utils.py tests/test_pat_webui_diarization_exports.py -q
```

端到端验证：
1. 启动 `FunASR_pat.bat`，确认 API + WebUI 正常
2. 用 curl 测试 `/health`、`/v1/models` 返回格式
3. 上传音频测试 `/v1/audio/transcriptions`
4. 浏览器打开 WebUI 测试离线/流式/说话人分离/情感识别
